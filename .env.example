# ContactCore is offline-first and requires no secrets.
# Optional absolute path override for the local database.
CONTACTCORE_DATA_PATH=
# Optional SQLCipher-compatible key. Prefer entering this at runtime instead of storing it here.
CONTACTCORE_DATABASE_KEY=
