# ContactCore is offline-first and requires no secrets.
# Optional absolute directory override for local ContactCore data.
# The database will be stored as <CONTACTCORE_DATA_PATH>/contactcore.db.
CONTACTCORE_DATA_PATH=
# Optional SQLCipher-compatible key read only at runtime.
# Do not place a real key in this file or commit one to Git.
CONTACTCORE_DATABASE_KEY=
